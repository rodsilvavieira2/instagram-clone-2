import styled from "styled-components";

import { BaseButton } from "../../../buttons";

export const EmojiButton = styled(BaseButton)`
  flex-shrink: 0;
`;

export const Container = styled.span`
  display: flex;
  align-items: center;
  justify-content: center;

  position: relative;
`;
